import React from 'react'

const LoadingSpinner = () => (
  <div className = "Loading-spinner">loading....</div>
  //<img className="loading-spinner" alt="Loading..." src ='../public/' />
)

export default LoadingSpinner